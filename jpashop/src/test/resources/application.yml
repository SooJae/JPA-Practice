spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 테이블을 지우고 다시 생성
#    properties:
#      hibernate:
#        #        show_sql: true system.out.println 으로 찍으므로 사실 사용하면 안된다.
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

